package com.project.dodung;

public class dictionaryInput {
    /**
     * If the given input is found -> Print the String.
     * Otherwise, print the f****** "Not found!".
     *
     * @param input The String to be searched.
     * @return whether the String is found or not.
     */

    public String findWord(String input) {
        String ans = "";
        if (!ans.equals("")) {
            return ans;
        } else {
            return "Not found";
        }
    }

}
